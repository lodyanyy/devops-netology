server_tokens off;

server {
    listen 80;
    server_name {{ domain_name }};

    location /.well-known/acme-challenge {
        root /var/www/letsencrypt;
        try_files $uri $uri/ =404;
    }

    location / {
        rewrite ^ https://{{ domain_name }}$request_uri? permanent;
    }
}

server {
    listen 80;
    server_name gitlab.lodyanyy.ru;

    location /.well-known/acme-challenge {
        root /var/www/letsencrypt;
        try_files $uri $uri/ =404;
    }

    location / {
        rewrite ^ https://gitlab.lodyanyy.ru$request_uri? permanent;
    }
}
server {
    listen 80;
    server_name grafana.lodyanyy.ru;

    location /.well-known/acme-challenge {
        root /var/www/letsencrypt;
        try_files $uri $uri/ =404;
    }

    location / {
        rewrite ^ https://grafana.lodyanyy.ru$request_uri? permanent;
    }
}
server {
    listen 80;
    server_name prometheus.lodyanyy.ru;

    location /.well-known/acme-challenge {
        root /var/www/letsencrypt;
        try_files $uri $uri/ =404;
    }

    location / {
        rewrite ^ https://prometheus.lodyanyy.ru$request_uri? permanent;
    }
}
server {
    listen 80;
    server_name alertmanager.lodyanyy.ru;

    location /.well-known/acme-challenge {
        root /var/www/letsencrypt;
        try_files $uri $uri/ =404;
    }

    location / {
        rewrite ^ https://alertmanager.lodyanyy.ru$request_uri? permanent;
    }
}
